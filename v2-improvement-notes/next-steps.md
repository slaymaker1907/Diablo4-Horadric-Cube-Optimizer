# Next Steps

Phase 8 is complete.

If later work resumes, start with these explicitly deferred follow-ups:

1. Revisit the residual solver if real browser cases still hit the largest configured abstract-state budget; the next architectural step is sparse expansion rather than another cap increase.
2. Add slot-specific roll weights or class refinements if the model needs to move beyond the current legality-only slot filtering.
3. Add value-threshold and material-cost objectives only as a deliberate new scope decision, not as silent post-processing.
4. Treat any future diagnostics or contract rename as explicit post-Phase-8 work and update [decisions.md](decisions.md) before landing it.